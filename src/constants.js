const IGNORED_IPS = new Set(["127.0.0.1", "255.255.255.255", "0.0.0.0"]);
const MAX_DISPLAY_RESULTS = 30;
const SUCCESS_CODES = [200, 404];

module.exports = { IGNORED_IPS, MAX_DISPLAY_RESULTS, SUCCESS_CODES };
